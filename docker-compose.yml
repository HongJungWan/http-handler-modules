version: "3.9"
# ref: https://docs.docker.com/compose/compose-file
services:
  app:
    image: http-handler-modules
    build:
      args:
        - target=dev
    volumes:
      - .:/app
    ports:
      - "18000:80"